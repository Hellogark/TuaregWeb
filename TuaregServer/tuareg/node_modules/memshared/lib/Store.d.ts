export interface Message {
    messageId: string;
    cmd: string;
    args?: any[];
    result?: any;
    error?: any;
}
export declare class Store {
    private $callbacks;
    dispatch(cmd: string, callback: Function, ...args: any[]): void;
    consume(message: Message): void;
    private _buildMessage(command, ...args);
}
